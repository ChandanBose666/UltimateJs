{
  "name": "@blazefw/native",
  "version": "0.1.1",
  "description": "BlazeFW native renderer — maps Stack, Text, Action, and Input primitives to React Native View, Text, Pressable, and TextInput with dp units and a built-in DEFAULT_THEME palette",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@blazefw/primitives": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-native": ">=0.72.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
