[package]
name = "nexus-crdt"
version = "0.1.0"
edition = "2021"
description = "Automerge CRDT document compiled to WebAssembly — the Zero-Fetch Sync engine for Nexus.js"
repository = "https://github.com/ChandanBose666/NexusJs"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
automerge = { version = "0.8", features = ["wasm"] }
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
js-sys = "0.3"

[dev-dependencies]
wasm-bindgen-test = "0.3"
