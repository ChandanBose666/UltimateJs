{
  "name": "@blazefw/primitives",
  "version": "0.1.0",
  "description": "BlazeFW semantic UI type contracts — Stack, Text, Action, and Input primitives with design-token-based spacing and color for web, native, and email renderers",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "publishConfig": { "access": "public" }
}
