{
  "name": "@blazefw/vite-plugin",
  "version": "0.1.0",
  "description": "BlazeFW Vite plugin — intercepts .ultimate.tsx files and routes them through the Rust compiler binary via stdin/stdout JSON bridge, splitting each file into server and client bundles automatically",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": { "access": "public" }
}
